# CLAUDE.md

このファイルは AI エージェント（Claude など）がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

Kaggle コンペティション用のテンプレートリポジトリ。Hydra + Wandb で実験管理を行い、FastAPI + htmx でダッシュボードを構築する。

## ディレクトリ構成

```
kaggle-template/
├── input/          # データ格納（gitignore）
├── output/         # 出力格納（gitignore）
├── sandbox/        # AI Agent 検証用（gitignore）
├── notebook/       # Jupyter Notebook（公開Code、検証用）
├── app/            # Web アプリ（FastAPI + htmx）
├── docs/           # ドキュメント
│   ├── official/   # Kaggle 公式情報
│   ├── discussion/ # Kaggle Discussion 情報
│   └── insights/   # 実験から得た知見
└── src/            # 実験ディレクトリ
    └── exp000-sample/
```

## 実験ディレクトリの規則

### 命名規則

`exp{番号}-{subtitle}` の形式を使用する。

- `exp000-sample` - サンプル実験
- `exp001-baseline` - ベースライン
- `exp002-feature-engineering` - 特徴量エンジニアリング

### ディレクトリ構成

各実験ディレクトリは独立しており、他の実験に依存しない。

```
src/exp001-xxx/
├── README.md       # 実験の概要・仮説・結果・考察
├── train.py        # 学習スクリプト
├── model.py        # モデル定義
├── data.py         # データ処理
└── config/         # Hydra 設定
    └── config.yaml
```

各実験の `README.md` には以下を記載する：

- **目的・仮説**: この実験で何を検証するか
- **実行方法**: 実験を再現するためのコマンド
- **結果**: CV スコア、LB スコア
- **考察**: 結果から何がわかったか

### 依存関係

- 実験ディレクトリ内のコードは、同じディレクトリ内のファイルにのみ依存する
- 共通ライブラリが必要な場合は、各実験ディレクトリにコピーする

### 探索の独立性（AI エージェントへの注意）

LLM は過去の実験結果を参照し続けることで、**アイデアが画一的になり、探索空間を自ら狭めてしまう**傾向がある。特に「うまくいかなかった実験」に引きずられて、有効な可能性がある手法を早期に除外してしまうリスクがある。

**過去の実験から引き継いでよいもの（コードレベルの知見のみ）:**
- データ前処理・特徴量生成の実装上の工夫
- バグ修正・速度改善の技術的な発見
- `docs/insights/` に記録された具体的な実装上の知見

**過去の実験から引き継いではいけないもの:**
- 「このアプローチはうまくいかない」という結論（条件・設定・組み合わせが変われば有効になりうる）
- 特定の手法やモデルへの偏り・先入観
- 探索する手法・ハイパーパラメータ・特徴量の範囲の絞り込み

**新しい実験を設計するときは:**
- 過去の実験履歴ではなく、**問題の本質・データの特性・ドメイン知識**から仮説を立てる
- 「なぜこの手法が有効か」という仮説を毎回ゼロベースで明確にする
- うまくいかなかった手法も、異なる前処理・設定・アーキテクチャとの組み合わせで再検討する価値がある

### 再現性

実験の再現性を保つため、以下を徹底する：

- **シード固定**: random、numpy、torch など使用するライブラリのシードを固定する
- **シード値の記録**: 使用したシード値は `config.yaml` に記載し、バージョン管理する
- **環境の記録**: 主要ライブラリのバージョンを記録する（`uv.lock` で管理）

### デバッグモード

新しい実験を作成する際は、必ずデバッグモードを実装する。デバッグモードにより、コードの動作検証を高速に行い、素早いイテレーションサイクルで実験を回す。

**実装方針:**

- `config.yaml` に `debug: true/false` フラグを用意する
- デバッグモード時の挙動：
  - データを少量（例: 100件）に制限する
  - エポック数を最小（例: 1エポック）にする
  - Fold 数を最小（例: 1 Fold）にする
  - Wandb のログを無効化またはオフラインモードにする
- 実験の開発フローでは、まずデバッグモードでパイプライン全体が通ることを確認してから、本番設定で実行する

**Hydra での設定例:**

```yaml
# config.yaml
debug: true
training:
  n_samples: 100    # debug=false 時は null（全データ）に上書き
  n_epochs: 1       # debug=false 時は本番値に上書き
  n_folds: 1        # debug=false 時は本番値に上書き
wandb:
  mode: disabled    # debug=false 時は online に上書き
```

**実行例:**

```bash
# デバッグモードで実行（パイプライン全体の動作確認）
uv run python train.py

# 本番モードで実行
uv run python train.py debug=false
```

### 実験後の更新（必須）

新しい実験を作成・完了したら、以下を必ず更新する：

1. **各実験フォルダの README.md**
   - 実験開始時: 目的・仮説・実行方法を記載する
   - 実験完了時: 結果（CV・LB スコア）と考察を追記する

2. **リポジトリルートの README.md の Experiments テーブル**
   - 実験名、CV スコア、LB スコア、説明を追記
   - Experiment Tree（mermaid）に実験の派生関係を追加

3. **docs/insights/**
   - `YYYY-MM-DD_exp{番号}-{subtitle}.md` で知見を記録
   - 何を試したか、結果、考察、次に試すべきことを記載

## docs ディレクトリの規則

### official/

Kaggle 公式から提供される情報を記載。

### discussion/

Kaggle Discussion から得られた情報を記載。命名規則: `YYYY-MM-DD_topic.md`

### insights/

自分の実験から得られた知見を記載。命名規則: `YYYY-MM-DD_topic.md`

## sandbox ディレクトリ

AI Agent が Python/Bash スクリプトを書いて検証を行うためのディレクトリ。このディレクトリの内容は gitignore される。

## Git 規則

### ブランチ戦略

- デフォルトブランチ: `main`
- 実験ブランチ: `exp/{番号}-{subtitle}`（例: `exp/001-baseline`）
- 機能ブランチ: `feature/{機能名}`（例: `feature/add-augmentation`）
- 修正ブランチ: `fix/{内容}`（例: `fix/data-loader-bug`）

**ワークフロー:**
1. 新しい実験や機能は専用ブランチで作業
2. 動作確認後、`main` にマージ
3. 小さな修正やドキュメント更新は `main` に直接コミット可

### コミットの粒度

適切な粒度でコミットする：

- **1コミット = 1つの論理的な変更**
- 実験コードとドキュメント更新は分けてコミット
- 動作する状態でコミット（壊れた状態でコミットしない）

**コミットのタイミング:**
- 新しい実験ディレクトリを作成したとき
- 実験が完了してスコアが確定したとき
- README.md や docs を更新したとき
- バグを修正したとき

### コミットメッセージ

gitmoji を使用し、日本語で記述する。

例:
- `🎉 初期コミット`
- `✨ 新機能を追加`
- `🐛 バグを修正`
- `📝 ドキュメントを更新`
- `♻️ コードをリファクタリング`
- `🔧 設定ファイルを変更`
- `🧪 exp001-baseline を追加`
- `📊 exp001 の結果を記録（CV: 0.85, LB: 0.83）`

### Push

- 作業の区切りごとに push する
- 1日の終わりには必ず push する
- 実験が完了したら push する
- 長時間作業する場合は WIP でも定期的に push する

## 技術スタック

- **実験管理**: Hydra, Wandb
- **Web アプリ**: FastAPI, htmx, Jinja2
- **パッケージ管理**: uv

## コマンド

```bash
# 依存関係インストール
uv sync

# Web アプリ起動
uv run uvicorn app.main:app --reload

# 実験実行
cd src/exp000-sample
uv run python train.py
```
